import React from "react";

const IconSuccess = (props) => (
  <i
    class="fas fa-check-circle"
    style={{ color: "#28a745", fontSize: "64px" }}
    {...props}
  ></i>
);

export default IconSuccess;
