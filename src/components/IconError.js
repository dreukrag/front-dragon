import React from "react";

const IconError = (props) => (
  <i
    class="fas fa-exclamation-circle"
    style={{ color: "#dc3545", fontSize: "64px" }}
    {...props}
  ></i>
);

export default IconError;
