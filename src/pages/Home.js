import React from "react";

const Home = () => (
  <div>
    <h1>Welcome to the dragon codex!</h1>
    <h5>You'll can find all the information about dragons below!</h5>
    <h5>But first you need to login!</h5>
  </div>
);

export default Home;
